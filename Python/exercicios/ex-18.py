import math
angulo = float(input('Angulo: '))
cos = round(math.cos(math.radians(angulo)),2)
sin = round(math.sin(math.radians(angulo)),2)

print(f'Valor do angulog: {angulo}')
print(f'Valor do cos: {cos}')
print(f'Valos do sin: {sin}')